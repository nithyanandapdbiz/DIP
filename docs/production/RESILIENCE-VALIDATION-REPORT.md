# Resilience validation report

**Every scenario below broke something on purpose.** A resilience test that does not
cause a failure is a happy-path test with a longer name.

| # | Scenario | Result | Observed |
|---|---|---|---|
| R-1 | a restarted process still trusts certificates issued before the restart | **PASS** | the certificate authority reloaded its state from disk and the existing certificate validates |
| R-2 | a restarted gateway serves credentials issued before the restart | **PASS** | served — certificate trust and token binding both survived the restart |
| R-3 | a connection interrupted mid-request does not destabilise the gateway | **PASS** | interruption observed as "interrupted"; the next request was served with status 200 |
| R-4 | an expired certificate is refused, and the refusal is classified | **PASS** | validation reason "expired" classified as lifecycle.expiry (major) |
| R-5 | a revoked secret version dies while the current version keeps working | **PASS** | v1 readable during overlap, gone after revocation; current is v2 |
| R-6 | one tenant's total failure does not affect another | **PASS** | tenant-a refused (401), tenant-b served (200) at the same time |
| R-7 | a rate-limited caller is refused and succeeds after the window | **PASS** | refused with 429 ("rate limit exceeded"), served on retry after the window (200) |
| R-8 | repeating an operation does not duplicate its effect | **PASS** | decommission returned true then false; re-registration returned the existing grant (idempotent: true) |
| R-9 | a decommissioned tenant can be re-provisioned and works again | **PASS** | re-provisioned tenant-b served with status 200 |
| R-10 | replay protection holds within a process, and the refusal names replay as the reason | **PASS** | first 200, replay 401 ("token replayed") |
| R-11 | replay protection holds ACROSS INSTANCES when a shared nonce store is supplied | **PASS** | shared store: first accepted, replay on the second instance refused as "replayed" |
| R-11.d | the platform reports when it is running single-process replay protection | **PASS** | the in-memory default reports singleProcessOnly=true, so a horizontally scaled deployment cannot run it unknowingly |

## Two defects this suite found

**Tokens did not survive a restart.** The token signing key was generated per
process, so a restarted instance rejected every token the previous one had issued
with `bad-signature`. A routine deploy was a customer outage. The key is now loaded
from state — the same custody model the certificate authority already used for its
root key, applied to the second key rather than a new scheme invented for it.

**Replay protection did not hold across instances.** Nonces were held in a
per-process map. Document 17 declares this plane *"multiple per region,
horizontally scaled"* and R-16.19 requires horizontal scalability, so a nonce
refused by one instance was accepted by another — **no restart required, a load
balancer was sufficient**. The store is now injectable and the platform reports
`replayProtectionIsSingleProcessOnly` so a deployment cannot run the default
unknowingly. Supplying a shared implementation is a deployment obligation (G-5).

### How the second one was nearly missed

The first version of the restart scenario asserted only that the replayed request
was refused. It was — with `bad-signature`, because of the *first* defect. A refusal
for an unrelated reason is a false pass, and it was concealing a real replay
exposure. Asserting on the **reason** rather than the status is what surfaced it.

---

*Generated by governance/production/run-production-evidence.js*
